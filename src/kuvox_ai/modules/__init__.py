"""Internal modules of the AI service.

Each module is a self-contained package with a public service class. Modules
talk to each other through these typed service interfaces only — never by
reaching into each other's internals.
"""
