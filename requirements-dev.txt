-r requirements.txt

# -----------------------------------------------------------------------------
# Testing
# -----------------------------------------------------------------------------
pytest==8.3.*
pytest-asyncio==0.24.*
pytest-cov==5.0.*
pytest-mock==3.14.*
pytest-xdist==3.6.*               # parallel test execution
httpx==0.27.*                     # async test client for FastAPI
respx==0.21.*                     # mock httpx responses
freezegun==1.5.*                  # mock time
faker==30.*                       # fake data

# -----------------------------------------------------------------------------
# Linting and formatting
# -----------------------------------------------------------------------------
ruff==0.7.*                       # linter + formatter, replaces flake8/black/isort
mypy==1.13.*
types-redis==4.6.*
types-pyyaml==6.0.*

# -----------------------------------------------------------------------------
# Documentation
# -----------------------------------------------------------------------------
mkdocs==1.6.*
mkdocs-material==9.5.*
mkdocstrings[python]==0.26.*

# -----------------------------------------------------------------------------
# Profiling and debugging
# -----------------------------------------------------------------------------
ipython==8.27.*
ipdb==0.13.*
py-spy==0.3.*                     # sampling profiler
memray==1.14.*; sys_platform != "win32"   # memory profiler, Linux/macOS only

# -----------------------------------------------------------------------------
# Notebook (for prototyping ML pipelines)
# -----------------------------------------------------------------------------
jupyter==1.1.*
notebook==7.2.*
matplotlib==3.9.*
seaborn==0.13.*
